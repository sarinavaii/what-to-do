import { ProjectsTemplate } from '@components/templates';

export const metadata = {
    title: 'Todo List',
    description: 'Your ordinary todo list application'
};

const Projects = () => {
    return <ProjectsTemplate />;
};

export default Projects;

