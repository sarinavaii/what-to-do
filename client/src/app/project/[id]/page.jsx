import { TasksTemplate } from '@components/templates';

export const metadata = {
    title: 'Todo List',
    description: 'Your ordinary todo list application'
};

function Tasks() {
    return <TasksTemplate />;
}

export default Tasks;
